#include "main.h"

/**
 * _isupper - check for uppercase
 * @c: number to be checked
 * Return: 1 for uppercase or 0 for otherwise
 */

int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
	return (1);
	}
	return (0);
}

